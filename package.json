{
  "name": "ghl-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that exposes GoHighLevel (contacts, opportunities, calendar, conversations) as tools for Claude Code",
  "type": "module",
  "bin": {
    "ghl-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  },
  "engines": {
    "node": ">=18"
  }
}
