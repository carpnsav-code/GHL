# Private Integration token from GHL: Settings > Other Settings > Private Integrations
GHL_API_TOKEN=

# The sub-account (location) this token is scoped to
GHL_LOCATION_ID=

# GHL API version header, only change if instructed by GHL API docs
GHL_API_VERSION=2021-07-28
